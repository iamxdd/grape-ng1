define([ "../../scripts/common/utils/date" ], function(dateUtils) {
    return {
        ctrlName: "product.list.view.ctrl",
        diList: [ "$scope", "$rootScope", "$http", "GrapeBaseLogic" ]
    };
});